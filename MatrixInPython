"""
Изучаю я как-то Питон и вначале моего пути попадается мне задачка :
Напишите программу, на вход которой подаётся прямоугольная матрица в виде последовательности строк, заканчивающихся строкой,
содержащей только строку "end" (без кавычек)
Программа должна вывести матрицу того же размера, у которой каждый элемент в позиции i, j равен сумме элементов первой матрицы
на позициях (i-1, j), (i+1, j), (i, j-1), (i, j+1). У крайних символов соседний элемент находится с противоположной стороны матрицы.
В случае одной строки/столбца элемент сам себе является соседом по соответствующему направлению.
и тут я понимаю что вот и началось настоящее обучение... в итоге с помощью Гугла и метода проб и ошибок получилось это:///
"""
a = '' #создаю переменные
b = []
while True:        
    a = str(input())
    if a == 'end':
        break
    b.append([int(s) for s in a.split()]) #делаю ввод строки сразу проверяю не последняя ли она и если нет то помещаю ее в список
li, lj = len(b), len(b[0]) # определяем длину самого списка и элемента в нем
new = [[sum([b[i-1][j], b[(i+1) % li][j], b[i][j-1], b[i][(j+1) % lj]]) for j in range(lj)] for i in range(li)] #делаем вычисление заданные
в примере и не забываем про(%) на это много много тупил
for i in range(li):
    for j in range(lj):
        print(new[i][j], end=' ')
    print() #ну и принт с помощью for
